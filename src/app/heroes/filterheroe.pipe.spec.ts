import { FilterheroePipe } from './filterheroe.pipe';

describe('FilterheroePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterheroePipe();
    expect(pipe).toBeTruthy();
  });
});
