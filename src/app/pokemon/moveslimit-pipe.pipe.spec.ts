import { MoveslimitPipePipe } from './moveslimit-pipe.pipe';

describe('MoveslimitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MoveslimitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
